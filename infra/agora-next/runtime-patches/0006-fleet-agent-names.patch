--- a/src/components/shared/ENSName.tsx
+++ b/src/components/shared/ENSName.tsx
@@ -1,6 +1,7 @@
 "use client";
 
 import { useEffect, useState } from "react";
+import { fleetAgentName } from "@/lib/fleetAgents";
 import { useEnsName } from "wagmi";
 import { CheckCircleIcon } from "@heroicons/react/20/solid";
 import { Copy } from "lucide-react";
@@ -24,6 +25,7 @@
   truncate = true,
   includeCtoC = false,
 }: ENSNameProps) {
+  const fleetName = fleetAgentName(address);
   const [ensName, setEnsName] = useState(
     truncate ? truncateAddress(address || "") : address || ""
   );
@@ -32,6 +34,7 @@
   const { data } = useEnsName({
     chainId: 1,
     address: address as `0x${string}`,
+    query: { enabled: !fleetName },
   });
 
   useEffect(() => {
@@ -54,11 +57,11 @@
     };
   }, [isInCopiedState]);
 
-  const displayText = truncate
+  const displayText = fleetName || (truncate
     ? ensName || truncateAddress(address)
-    : ensName || address;
+    : ensName || address);
 
-  const fullText = data || address;
+  const fullText = fleetName ? address : data || address;
 
   const copyToClipboard = (e: React.MouseEvent) => {
     e.preventDefault();
--- a/src/components/Votes/ProposalVotesList/ProposalSingleVote.tsx
+++ b/src/components/Votes/ProposalVotesList/ProposalSingleVote.tsx
@@ -1,3 +1,4 @@
 import type { Vote } from "@/app/api/common/votes/vote";
+import { fleetAgentName } from "@/lib/fleetAgents";
 import { useAccount } from "wagmi";
 import { HStack, VStack } from "@/components/Layout/Stack";
@@ -122,7 +122,7 @@
 
   const { data: ensFromBlockCache } = useBlockCacheWrappedEns({
     address: vote.address as `0x${string}`,
-    enabled: resolveEns && !vote.voterMetadata?.name,
+    enabled: resolveEns && !fleetAgentName(vote.address) && !vote.voterMetadata?.name,
   });
 
   const _onOpenChange = async (open: boolean) => {
@@ -134,7 +134,7 @@
     }
   };
 
-  const name = vote.voterMetadata?.name || ensFromBlockCache?.name;
+  const name = fleetAgentName(vote.address) || vote.voterMetadata?.name || ensFromBlockCache?.name;
   const zeroVpVote = isZeroVotingPowerVote(vote);
 
   const ensAvatar = () => {
