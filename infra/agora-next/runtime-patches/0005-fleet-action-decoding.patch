--- a/src/hooks/useTransactionDecoding.ts
+++ b/src/hooks/useTransactionDecoding.ts
@@ -2,6 +2,7 @@
 import { cachedDecodeEnhanced } from "@/lib/transactionDecoder";
 import { keccak256 } from "viem";
 import { toUtf8Bytes } from "ethers";
+import { decodeFleetDecisionCall } from "@/lib/fleetDecisionCall";
 
 export const useTransactionDecoding = (
   target: string,
@@ -52,6 +53,10 @@
         return null;
       }
 
+      if (process.env.NEXT_PUBLIC_AGORA_INSTANCE_NAME === "fleet") {
+        return decodeFleetDecisionCall(parsedCalldata as `0x${string}`);
+      }
+
       return await cachedDecodeEnhanced(
         target,
         parsedCalldata,
--- a/src/components/Proposals/ExecutionTransactions/ExecutionTransactions.tsx
+++ b/src/components/Proposals/ExecutionTransactions/ExecutionTransactions.tsx
@@ -76,9 +76,12 @@
       const data = await response.json();
       return data.transactions || [];
     },
-    enabled: !!proposalId && !!tenant,
+    enabled: !!proposalId && !!tenant && tenant !== "fleet",
     staleTime: 5 * 60 * 1000, // Cache for 5 minutes
   });
+
+  // Fleet records resource execution in the linked run evidence, not this optional database.
+  if (tenant === "fleet") return null;
 
   if (loading) {
     return (
